# file: .github/workflows/coverage.yml
# version: 1.1.2
# guid: 4f094b99-d830-465a-b726-40ac118cd985

name: Code Coverage

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  code-quality: write

jobs:
  coverage:
    name: Rust Coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}

      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
        with:
          toolchain: "1.87"
          components: llvm-tools-preview
          cache: true

      - name: Install cargo-llvm-cov
        run: cargo install cargo-llvm-cov --locked

      - name: Generate Cobertura coverage
        run: cargo llvm-cov --workspace --locked --cobertura --output-path coverage.xml

      - name: Upload coverage report
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
        uses: actions/upload-code-coverage@d8e329117199404bba6fc81efe8093dc7c015e34 # v1.4.2
        with:
          # The published v1 action metadata requires `file`; the docs example
          # currently says `report`, which would fail action input validation.
          file: coverage.xml
          language: Rust
          label: code-coverage/cargo-llvm-cov
          fail-on-error: false
          token: ${{ github.token }}
